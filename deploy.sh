#!/bin/bash

# Быстрый деплой без пересборки образа
# Использует volumes для монтирования кода

set -e

echo "🚀 Быстрый деплой бота..."

# Проверяем что мы на сервере
if [ ! -f ".env" ]; then
    echo "❌ Файл .env не найден!"
    echo "Скопируйте .env.example в .env и заполните переменные"
    exit 1
fi

# Получаем последние изменения
echo "📥 Получаем изменения из git..."
git pull

# Очищаем Python кэш на хосте (важно для обновления кода!)
echo "🧹 Очищаем Python кэш..."
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
find . -type f -name "*.pyc" -delete 2>/dev/null || true

# Применяем миграции (если есть)
echo "🔄 Применяем миграции БД..."
docker-compose exec -T app alembic upgrade head || echo "⚠️  Не удалось применить миграции (возможно контейнер не запущен)"

# Перезапускаем только app и worker (БД и Redis не трогаем)
# Используем --force-recreate чтобы пересоздать контейнеры и очистить Python кэш
echo "♻️  Пересоздаём контейнеры..."
docker-compose up -d --no-deps --force-recreate app worker

echo "✅ Деплой завершён!"
echo ""
echo "📊 Статус воркеров:"
docker ps --filter "name=worker" --format "table {{.Names}}\t{{.Status}}"
echo ""
echo "📊 Проверьте логи:"
echo "   docker-compose logs -f app"
echo "   docker-compose logs -f worker"
