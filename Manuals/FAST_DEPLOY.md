# Быстрый деплой без пересборки Docker 🚀

## Проблема

Каждый раз при изменении кода приходится пересобирать Docker образ с `--no-cache`, что занимает 3-4 минуты.

## Решение

Используем **volumes** для монтирования кода — изменения применяются мгновенно без пересборки!

---

## 🎯 Три режима работы

### 1. **Быстрый деплой (рекомендуется для разработки)**

Код монтируется как volume, изменения применяются мгновенно.

```bash
# На сервере после git pull
./deploy.sh
```

Или вручную:
```bash
git pull
docker-compose restart app worker
```

**Время:** ~5-10 секунд ⚡

**Когда использовать:**
- Изменения в Python коде (bot/, handlers/, services/)
- Изменения в конфигурации
- Исправление багов
- Добавление новых функций

**Volumes уже настроены в docker-compose.yml:**
```yaml
volumes:
  - ./bot:/app/bot
  - ./alembic:/app/alembic
```

### 2. **Деплой с миграциями**

Если изменились модели БД:

```bash
git pull
docker-compose exec app alembic upgrade head
docker-compose restart app worker
```

**Время:** ~10-15 секунд

### 3. **Полная пересборка (только при изменении зависимостей)**

Только если изменился `requirements.txt`:

```bash
git pull
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

**Время:** ~3-4 минуты

**Когда использовать:**
- Добавлены новые Python пакеты в requirements.txt
- Изменился Dockerfile
- Обновление версий зависимостей

---

## 📋 Пошаговая инструкция

### Первоначальная настройка (один раз)

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd NanoBananaTgBot

# 2. Настройте .env
cp .env.example .env
nano .env

# 3. Первый запуск (с пересборкой)
docker-compose build
docker-compose up -d

# 4. Проверьте что всё работает
docker-compose logs -f
```

### Ежедневная работа (быстрый деплой)

```bash
# На локальной машине
git add .
git commit -m "Исправил баг в handlers"
git push

# На сервере
cd NanoBananaTgBot
./deploy.sh
```

Или вручную:
```bash
git pull
docker-compose restart app worker
```

**Готово!** Изменения применены за 5-10 секунд 🎉

---

## 🔧 Оптимизированный Dockerfile

Новый Dockerfile использует multi-stage build с двумя режимами:

### Development (для разработки)
```dockerfile
FROM python:3.11-slim as development
# Код монтируется как volume
# Hot reload включён
```

### Production (для продакшена)
```dockerfile
FROM python:3.11-slim as production
# Код запекается в образ
# Оптимизирован для размера
```

---

## 💡 Советы по оптимизации

### 1. Используйте volumes для разработки

Уже настроено в `docker-compose.yml`:
```yaml
volumes:
  - ./bot:/app/bot          # Код бота
  - ./alembic:/app/alembic  # Миграции
```

### 2. Кэшируйте зависимости

Dockerfile оптимизирован — `requirements.txt` копируется отдельно:
```dockerfile
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
# Зависимости кэшируются, пока requirements.txt не изменится
```

### 3. Используйте .dockerignore

Создайте `.dockerignore` чтобы не копировать лишнее:
```
__pycache__
*.pyc
*.pyo
*.pyd
.git
.gitignore
.env
.venv
venv/
*.log
.DS_Store
.pytest_cache
.hypothesis
```

### 4. Перезапускайте только нужные сервисы

```bash
# Только app
docker-compose restart app

# Только worker
docker-compose restart worker

# Оба (но не БД и Redis)
docker-compose restart app worker
```

---

## 🐛 Troubleshooting

### Изменения не применяются

**Проблема:** Код изменился, но бот работает по-старому.

**Решение:**
```bash
# 1. Проверьте что volumes настроены
docker-compose config | grep volumes

# 2. Перезапустите контейнеры
docker-compose restart app worker

# 3. Если не помогло — пересоберите
docker-compose down
docker-compose up -d --build
```

### Ошибка "Module not found"

**Проблема:** Добавили новый пакет в requirements.txt.

**Решение:** Нужна полная пересборка:
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Worker не видит изменения

**Проблема:** Worker процесс не перезагружается автоматически.

**Решение:**
```bash
docker-compose restart worker
```

---

## 📊 Сравнение времени

| Метод | Время | Когда использовать |
|-------|-------|-------------------|
| `./deploy.sh` | 5-10 сек | Изменения в коде |
| `docker-compose restart` | 5-10 сек | Изменения в коде |
| С миграциями | 10-15 сек | Изменения в моделях БД |
| Полная пересборка | 3-4 мин | Изменения в requirements.txt |

---

## 🎓 Дополнительные команды

```bash
# Просмотр логов в реальном времени
docker-compose logs -f app

# Проверка статуса
docker-compose ps

# Остановка всех сервисов
docker-compose down

# Запуск с пересборкой
docker-compose up -d --build

# Очистка старых образов
docker system prune -a
```

---

## ✅ Чеклист перед деплоем

- [ ] Код протестирован локально
- [ ] Изменения закоммичены в git
- [ ] .env файл актуален на сервере
- [ ] Если изменились модели — есть миграция
- [ ] Если изменился requirements.txt — нужна пересборка

---

## 🚀 Автоматизация (опционально)

Можно настроить GitHub Actions для автоматического деплоя:

```yaml
# .github/workflows/deploy.yml
name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /path/to/NanoBananaTgBot
            git pull
            docker-compose restart app worker
```

---

Теперь деплой занимает секунды вместо минут! 🎉
