# Pre-Launch Checklist ✅

Используйте этот чеклист перед запуском бота в production.

## Обязательные шаги

### 1. Переменные окружения
- [ ] `.env` файл создан из `.env.example`
- [ ] `BOT_TOKEN` заполнен (от @BotFather)
- [ ] `OPENAI_API_KEY` заполнен (от platform.openai.com)
- [ ] `WEBHOOK_URL` указывает на ваш домен с HTTPS
- [ ] `POSTGRES_PASSWORD` изменен на сильный пароль

### 2. Инфраструктура
- [ ] Docker установлен (`docker --version`)
- [ ] Docker Compose установлен (`docker-compose --version`)
- [ ] Достаточно места на диске (минимум 20GB)
- [ ] Достаточно RAM (минимум 2GB)

### 3. Сеть и домен
- [ ] Домен настроен и указывает на сервер
- [ ] SSL сертификат установлен (Let's Encrypt или другой)
- [ ] Порты 80 и 443 открыты
- [ ] Nginx настроен как reverse proxy

### 4. Безопасность
- [ ] `.env` файл не в git (проверьте `.gitignore`)
- [ ] Firewall настроен (только 22, 80, 443)
- [ ] SSH доступ по ключу (не по паролю)
- [ ] Пароль PostgreSQL сильный и уникальный

## Проверка перед запуском

### Локальная проверка

```bash
# Проверка конфигурации
docker-compose config

# Проверка Dockerfile
docker build -t test-build .

# Проверка .env файла
cat .env | grep -v "^#" | grep "="
```

### Тестовый запуск

```bash
# Запуск в foreground режиме (для проверки логов)
docker-compose up

# Если всё ОК, остановите (Ctrl+C) и запустите в background
docker-compose up -d
```

### Проверка работоспособности

```bash
# Health check
curl http://localhost:8000/health

# Проверка webhook (замените на ваш домен)
curl https://your-domain.com/health

# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f
```

### Проверка базы данных

```bash
# Подключение к PostgreSQL
docker-compose exec postgres psql -U botuser -d telegram_bot

# Проверка таблиц
\dt

# Выход
\q
```

### Проверка Redis

```bash
# Проверка Redis
docker-compose exec redis redis-cli ping
# Должно вернуть: PONG
```

## После запуска

### Telegram бот
- [ ] Отправьте `/start` боту в Telegram
- [ ] Проверьте регистрацию пользователя
- [ ] Проверьте главное меню
- [ ] Попробуйте Создать картинку с нуля (тестовый промпт)

### Мониторинг
- [ ] Настройте автоматический backup БД
- [ ] Настройте мониторинг логов
- [ ] Настройте алерты на ошибки (опционально)

### Документация
- [ ] Сохраните учетные данные в безопасном месте
- [ ] Задокументируйте процесс обновления
- [ ] Создайте runbook для типичных проблем

## Production Best Practices

### Резервное копирование
```bash
# Настройте ежедневный backup
crontab -e

# Добавьте:
0 3 * * * /path/to/backup.sh
```

### Мониторинг ресурсов
```bash
# Проверяйте использование ресурсов
docker stats

# Проверяйте использование диска
df -h
docker system df
```

### Обновления
```bash
# Регулярно обновляйте систему
sudo apt update && sudo apt upgrade -y

# Обновляйте Docker образы
docker-compose pull
docker-compose up -d
```

## Troubleshooting

Если что-то не работает:

1. **Проверьте логи:**
   ```bash
   docker-compose logs -f
   ```

2. **Проверьте статус:**
   ```bash
   docker-compose ps
   ```

3. **Проверьте конфигурацию:**
   ```bash
   docker-compose config
   ```

4. **Перезапустите сервисы:**
   ```bash
   docker-compose restart
   ```

5. **Пересоздайте контейнеры:**
   ```bash
   docker-compose down
   docker-compose up -d
   ```

## Контакты и поддержка

- 📖 Документация: [README.md](README.md)
- 🚀 Deployment: [DEPLOYMENT.md](DEPLOYMENT.md)
- ⚡ Быстрый старт: [QUICKSTART.md](QUICKSTART.md)

---

**Важно:** Не пропускайте шаги из раздела "Безопасность"! 🔒
