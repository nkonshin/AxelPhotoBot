# Changelog: Улучшения и рефакторинг

## Дата: 28 декабря 2025, 15:30

### 20. Отправка всех изображений в OpenAI API

**Файл:** `bot/services/image_provider.py`

Исправлена передача изображений в OpenAI edit endpoint. Теперь передаётся массив изображений (до 16 штук) вместо только первого.

```python
# Было:
image=primary_image

# Стало:
image_param = image_files if len(image_files) > 1 else image_files[0]
image=image_param
```

### 21. Исправлено HTML форматирование результата

**Файл:** `bot/tasks/generation.py`

Добавлен `parse_mode="HTML"` в `send_document` для корректного отображения тегов `<b>`, `<blockquote>`, `<i>`.

### 22. Упрощено имя файла

**Файл:** `bot/tasks/generation.py`

Имя файла изменено с `GPT_Image_{task_id}.png` на простое `GPT_Image.png`.

### 23. Анимация генерации

**Файл:** `bot/tasks/generation.py`

Добавлено сообщение "⏳ Генерирую..." которое отправляется при начале генерации и автоматически удаляется после получения результата или ошибки.

Новые функции:
- `_send_animation_message()` - отправка анимационного сообщения
- `_delete_animation_message()` - удаление сообщения после завершения

### 24. Уведомления админов об ошибках

**Файл:** `bot/tasks/generation.py`

При ошибке генерации теперь отправляется уведомление всем админам из `ADMIN_IDS` с информацией:
- Task ID
- User ID и Telegram ID
- Username
- Тип задачи и модель
- Промпт (первые 200 символов)
- Текст ошибки

Новая функция: `_notify_admins_about_error()`

### 25. Обновлён текст получения фото

**Файл:** `bot/handlers/edit.py`

Новый текст при получении фото:
```
✅ Фото получено! (1/10)

Опишите, что нужно сделать с изображением.

💡 Примеры:
• «Сделай портрет в студийном свете»
• «Замени фон на город ночью с неоновыми огнями»
• «Создай атмосферу как в кино: мягкий свет, глубина резкости»

💡 Если вы загрузили несколько фото, вы можете:
• «Соедини человека с первого фото с локацией со второго»
• «Собери одно изображение, используя лучшие детали из всех фото»
• «Возьми стиль освещения с одного снимка и примени к другому»

📎 Можете добавить ещё фото (до 10) или отправьте описание
```

### 26. Динамический заголовок для 2+ фото

**Файл:** `bot/handlers/edit.py`

При загрузке 2+ фото заголовок меняется на "✅ Получено фото: 2/10" вместо "✅ Фото получено! (1/10)".

### 27. Batch-загрузка фото

**Файл:** `bot/handlers/edit.py`

При отправке нескольких фото за раз (media group) теперь приходит одно сообщение вместо отдельного на каждое фото. Реализовано через отслеживание `media_group_id`.

### 28. Сброс изображений при нажатии "Назад"

**Файл:** `bot/handlers/menu.py`

Подтверждено, что обработчик `back_to_menu` вызывает `state.clear()`, что сбрасывает все загруженные изображения при возврате в меню.

---

## Дата: 26 декабря 2025

### 1. Централизация констант в config.py

**Было:** `HIGH_COST_THRESHOLD = 4000` дублировался в 3 файлах

**Стало:** Единая переменная `config.high_cost_threshold` из ENV

**Файлы:**
- `bot/config.py` — добавлены новые настройки
- `bot/handlers/generate.py` — использует `config.high_cost_threshold`
- `bot/handlers/edit.py` — использует `config.high_cost_threshold`
- `bot/handlers/trends.py` — использует `config.high_cost_threshold`

### 2. Новые переменные окружения

| Переменная | Описание |
|------------|----------|
| `HIGH_COST_THRESHOLD` | Порог для двойного подтверждения (default: 4000) |
| `MAX_TASKS_PER_USER_PER_HOUR` | Rate limiting (default: 20) |
| `ADMIN_IDS` | Telegram ID админов через запятую |
| `ADMIN_API_KEY` | API ключ для HTTP админ-эндпоинтов |

### 3. Сервис создания задач (task_service.py)

**Новый файл:** `bot/services/task_service.py`

Централизует логику создания задач:
- Проверка rate limiting
- Списание токенов
- Создание задачи в БД
- Постановка в очередь RQ

### 4. Репозиторий статистики (StatsRepository)

**Файл:** `bot/db/repositories.py`

Новые методы:
- `get_total_users()` — всего пользователей
- `get_total_tasks()` — всего задач
- `get_tasks_by_status()` — задачи по статусам
- `get_total_tokens_spent()` — потрачено токенов
- `get_tasks_today()` — задач сегодня
- `get_users_today()` — новых пользователей сегодня
- `get_active_users_today()` — активных сегодня
- `get_top_users()` — топ пользователей
- `get_model_usage()` — использование моделей
- `get_full_stats()` — полная статистика

### 5. Rate Limiting

**Файл:** `bot/db/repositories.py`

Новый метод `count_user_tasks_since(user_id, hours)` для подсчёта задач за период.

### 6. Админ-панель (Telegram)

**Новый файл:** `bot/handlers/admin.py`

Команды:
- `/admin` — меню с кнопками
- `/stats` — статистика
- `/addtokens <id> <amount>` — добавить токены
- `/userinfo <id>` — инфо о пользователе

### 7. Админ API (HTTP)

**Файл:** `bot/main.py`

Эндпоинты (требуют `X-Admin-API-Key`):
- `GET /admin/stats` — статистика
- `GET /admin/queue` — очередь RQ
- `GET /admin/users/{telegram_id}` — инфо о пользователе
- `POST /admin/users/{telegram_id}/tokens` — добавить токены

### 8. Обновлённые файлы

- `docker-compose.yml` — новые ENV переменные
- `.env.example` — полный пример конфигурации
- `DEVELOPER_GUIDE.md` — документация админ-панели
- `bot/handlers/__init__.py` — регистрация admin_router

### 9. Исправление gpt-image-1.5 для edit

**Файлы:**
- `bot/services/image_provider.py` — убран fallback
- `bot/handlers/edit.py` — убран fallback

Теперь `gpt-image-1.5` поддерживается для редактирования (согласно документации OpenAI).

---

## Как настроить админку

1. Узнай свой Telegram ID (напиши боту @userinfobot)

2. Добавь в `.env`:
```env
ADMIN_IDS=твой_telegram_id
ADMIN_API_KEY=сгенерируй_случайную_строку
```

3. Перезапусти бота:
```bash
docker-compose build --no-cache
docker-compose up -d
```

4. Напиши боту `/admin`


---

## Дата: 28 декабря 2025

### 10. Удалён ID задачи из сообщений об успехе

**Файлы:**
- `bot/handlers/generate.py`
- `bot/handlers/edit.py`
- `bot/handlers/trends.py`

Убрана строка `🆔 ID задачи: <code>{task.id}</code>` из сообщений о создании задачи.

### 11. Поддержка нескольких фото для редактирования (до 10)

**Файлы:**
- `bot/handlers/edit.py` — сбор нескольких фото, +10% за каждое дополнительное
- `bot/services/image_provider.py` — поддержка JSON-массива file_id
- `bot/states/generation.py` — обновлены комментарии

Пользователь может отправить до 10 изображений. Каждое дополнительное добавляет +10% к стоимости.

### 12. Редактирование через ответ на сгенерированное изображение

**Файл:** `bot/handlers/edit.py`

Новый хендлер `handle_reply_to_edit` — если пользователь отвечает на сообщение бота с документом (сгенерированной картинкой) текстом, автоматически запускается flow редактирования.

### 13. Меню команд бота

**Файл:** `bot/main.py`

При старте бота устанавливаются команды через `set_my_commands`:
- `/start` — Запустить бота
- `/balance` — Проверить баланс
- `/guide` — Руководство
- `/support` — Поддержка
- `/invite` — Пригласить друга

### 14. Улучшенный формат результата

**Файл:** `bot/tasks/generation.py`

- Промпт в `<blockquote>`
- Имя файла: `GPT_Image_{task_id}.png`
- Подсказка о reply-to-edit
- Кнопка «Сгенерировать ещё»

### 15. Команда /support

**Новый файл:** `bot/handlers/support.py`

Показывает контакт поддержки из `SUPPORT_USERNAME`.

**Файл:** `bot/config.py` — добавлена переменная `support_username`

### 16. Кнопка «Сгенерировать ещё»

**Файлы:**
- `bot/keyboards/inline.py` — `regenerate_keyboard()`, `REGENERATE_PREFIX`
- `bot/handlers/regenerate.py` — обработчик кнопки

При нажатии загружает настройки исходной задачи и показывает экран подтверждения.

### 17. Реферальная система

**Файлы:**
- `bot/db/models.py` — поле `referrer_id` в User
- `bot/db/repositories.py` — `get_referral_stats()`, обновлён `get_or_create()`
- `bot/handlers/start.py` — парсинг `ref_USERID` из deep link
- `bot/handlers/invite.py` — команда `/invite`
- `alembic/versions/d9e2f3a4b5c6_add_referrer_id_to_users.py` — миграция

Формат ссылки: `https://t.me/bot_username?start=ref_USERID`

### 18. Новые переменные окружения

| Переменная | Описание |
|------------|----------|
| `SUPPORT_USERNAME` | Username поддержки (например @support) |

### 19. Обновлённые файлы

- `bot/handlers/__init__.py` — регистрация support_router, invite_router, regenerate_router
- `.env.example` — добавлен SUPPORT_USERNAME

---

## Как применить миграцию

```bash
# Локально
alembic upgrade head

# В Docker
docker-compose exec app alembic upgrade head
```
