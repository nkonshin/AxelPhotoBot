# Быстрый старт за 5 минут ⚡

## Предварительные требования

- Docker и Docker Compose установлены
- Telegram Bot Token от [@BotFather](https://t.me/BotFather)
- OpenAI API Key от [platform.openai.com](https://platform.openai.com)
- (Опционально) Канал для проверки подписки

## Шаги

### 1️⃣ Клонируйте репозиторий

```bash
git clone <repository-url>
cd NanoBananaTgBot
```

### 2️⃣ Настройте переменные окружения

```bash
cp .env.example .env
nano .env  # или vim, code, любой редактор
```

Минимально необходимые переменные:

```bash
BOT_TOKEN=ваш_токен_от_BotFather
OPENAI_API_KEY=ваш_ключ_OpenAI
WEBHOOK_URL=https://ваш-домен.com  # или ngrok URL для локальной разработки

# Админы (ваш Telegram ID)
ADMIN_IDS=123456789

# Поддержка
SUPPORT_USERNAME=@your_support

# Подписка (опционально)
SUBSCRIPTION_CHANNEL=@nkonshin_ai
SUBSCRIPTION_REQUIRED=true

# Видео-приветствие (опционально, получите file_id отправив видео-кружок боту как админ)
WELCOME_VIDEO_FILE_ID=
```

### 3️⃣ Запустите бота

```bash
docker-compose up -d
```

Или используйте Makefile:

```bash
make up
```

### 4️⃣ Проверьте логи

```bash
docker-compose logs -f
```

Или:

```bash
make logs
```

### 5️⃣ Готово! 🎉

Откройте Telegram и отправьте `/start` вашему боту.

## Локальная разработка с ngrok

Для локальной разработки используйте ngrok для создания публичного URL:

```bash
# В отдельном терминале
ngrok http 8000
```

Скопируйте URL от ngrok (например, `https://abc123.ngrok.io`) и обновите `WEBHOOK_URL` в `.env`:

```bash
WEBHOOK_URL=https://abc123.ngrok.io
```

Перезапустите контейнер:

```bash
docker-compose restart app
```

## Полезные команды

```bash
make help          # Показать все команды
make logs          # Логи всех сервисов
make logs-app      # Логи FastAPI
make logs-worker   # Логи Worker
make shell         # Открыть shell в контейнере
make down          # Остановить всё
make clean         # Удалить всё (включая данные)
```

## Проверка работоспособности

```bash
# Health check
curl http://localhost:8000/health

# Информация о сервисе
curl http://localhost:8000/

# Статус контейнеров
docker-compose ps
```

## Что дальше?

- 📖 Полная документация: [README.md](../README.md)
- 🚀 Production deployment: [DEPLOYMENT.md](DEPLOYMENT.md)
- 🛠️ Developer Guide: [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md)
- 📝 Changelog: [CHANGELOG_IMPROVEMENTS.md](CHANGELOG_IMPROVEMENTS.md)
- 🧪 Запуск тестов: `make test`
- 🐛 Troubleshooting: проверьте логи с `make logs`

## Полезные фичи

### Получение file_id для видео-приветствия

Как админ, просто отправьте видео-кружок боту — он автоматически покажет file_id.

### Админ-панель

Напишите боту `/admin` для доступа к:
- Статистике бота
- Управлению пользователями
- Рассылкам
- Переключению проверки подписки

### Реферальная система

Пользователи могут получить реферальную ссылку через `/invite` и приглашать друзей.

## Частые проблемы

### Бот не отвечает

1. Проверьте логи: `make logs-app`
2. Убедитесь что WEBHOOK_URL правильный
3. Проверьте что BOT_TOKEN корректный

### Worker не обрабатывает задачи

1. Проверьте логи: `make logs-worker`
2. Убедитесь что Redis запущен: `docker-compose ps redis`
3. Проверьте OPENAI_API_KEY

### База данных не доступна

1. Проверьте статус: `docker-compose ps postgres`
2. Проверьте логи: `docker-compose logs postgres`
3. Перезапустите: `docker-compose restart postgres`

### Видео-кружок не отправляется

1. Убедитесь что `WELCOME_VIDEO_FILE_ID` заполнен в .env
2. Получите file_id: отправьте видео-кружок боту как админ
3. Бот автоматически покажет file_id — скопируйте в .env
4. Перезапустите: `docker-compose restart app`

### Проверка подписки не работает

1. Убедитесь что бот добавлен в канал как админ
2. Проверьте `SUBSCRIPTION_CHANNEL` в .env (формат: @channel_name)
3. Проверьте `SUBSCRIPTION_REQUIRED=true`
4. Перезапустите: `docker-compose restart app`

### Админ-команды не работают

1. Проверьте что ваш Telegram ID в `ADMIN_IDS`
2. Узнать свой ID: напишите @userinfobot
3. Формат: `ADMIN_IDS=123456789,987654321` (через запятую без пробелов)
4. Перезапустите: `docker-compose restart app`

## Поддержка

Если что-то не работает:
1. Проверьте логи: `make logs`
2. Проверьте конфигурацию: `docker-compose config`
3. Пересоздайте контейнеры: `make clean && make up`
